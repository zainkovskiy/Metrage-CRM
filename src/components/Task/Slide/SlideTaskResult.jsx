import React from 'react';
import { SliderBlock, SliderTitle } from '../../../styles/slider';
import styled from 'styled-components';

const SlideTaskResultStyle = styled(SliderBlock)``;
const SlideTaskResult = () => {
  return (
    <SliderBlock>
      <SliderTitle>Результат</SliderTitle>
    </SliderBlock>
  );
};

export default SlideTaskResult;
